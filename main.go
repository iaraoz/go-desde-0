package main

import (
	"github.com/iaraoz/go-desde-0/iteraciones"
)

func main() {
	/*estado, texto := variables.ConveritoaTexto(1986)
	fmt.Println(estado)
	fmt.Println(texto)*/
	/* os := runtime.GOOS

	if os == "linux" {
		fmt.Println("Linux")
	} else {
		fmt.Println("Parece Windows")
	}
	switch os {
	case "linux":
		fmt.Println("Identificado en el case, es linux")
	case "Windows":
		fmt.Printf("%s \n", os)
	}
	*/
	iteraciones.Iterar()
}
